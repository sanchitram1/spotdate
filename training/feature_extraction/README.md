# Feature Extraction

Let's make a notebook that's titled `GROUPING.ipynb`, where grouping is from the
spreadsheet we created. The output should ideally be users_{grouping}.csv, and then
we'll be joining on the user_ids. 