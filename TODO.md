# TODOs

## Training

- [x] cutoff date should be config and users should overlap across cutoff time
- [x] clustering should be executable from main and output all matches, top k matches
      and high quality matches (above a certain cutoff score)
- [ ] investigate the cluster / similarity score distribution
- [ ] scree plot on the KMeans clusterer
- [ ] README updates for the training folder to make the pipeline / flow clear
- [ ] re-run features df
- [ ] standardize the training notebooks and save the model, evals at the end